# Search-Field
- ## Installation
    1. Import the SearchField script into your webpage.
    2. If the magnifying glass icon doesn't render, import the Font Awesome library to your project.
    3. Create a 'hidden' class.
- ## Usage
    1. Create an element with the class `search-field` and add a `data-target` attribute with the value set to the `table_id`.